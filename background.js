/**
 * WhatsApp Kanban CRM — background.js
 * Service Worker (Manifest V3)
 * Responsável apenas por manter o ciclo de vida da extensão.
 * Toda a lógica fica em content.js.
 */

chrome.runtime.onInstalled.addListener(() => {});
